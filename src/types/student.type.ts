export interface IStudent {
  id: string;
  name: string;
  // .. etc
}

export interface IStudentData {
  id: string;
  studentName: string;
  age: number;
  educationLevel: string;
  subject: string;
  avatar?: string;
}
